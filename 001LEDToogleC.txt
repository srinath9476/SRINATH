/*
 * 001LEDToogle.c
 *
 *  Created on: Feb 9, 2021
 *      Author: lenoo
 */

#include "STM32F0G070rbtx.h"

uint32_t j=0;
void delay_ms()
{
	uint32_t i=0;
	for(i=0;i<5000;i++);
}
int main(void)
{
	GPIO_Handle_t GPIOLed;

	/**/
	GPIO_Regdef_t *GPIOAX5;

	GPIOLed.pGPIOxBaseAddr=GPIOA;
	GPIOLed.GPIO_PinConfig.GPIO_PinNumber=GPIO_PIN_NO_5;
	GPIOLed.GPIO_PinConfig.GPIO_PinMode=GPIO_MODE_OUT;
	GPIOLed.GPIO_PinConfig.GPIO_PinSpeed=GPIO_OP_SPEED_HD;
	GPIOLed.GPIO_PinConfig.GPIO_PinOPType=GPIO_OP_TYPE_PP;
	GPIOLed.GPIO_PinConfig.GPIO_PinPupdControl=GPIO_PL_NO;

	GPIO_PeriClockControl(GPIOA, ENABLE);
	GPIOx_Init(&GPIOLed);
	GPIOAX5 = GPIOA;

	while(1)
	{
	  /*GPIO_ToggleOutputPin(GPIOA, GPIO_PIN_NO_5);
	  delay_ms();*/

		//GPIOA->ODR^=1<<5;

		GPIOAX5->ODR^=1<<5;
		delay_ms();


	}
	return 0;
}
